export enum OauthProvider {
  GOOGLE = "google",
}

export enum KycStatus {
  NONE = "none",
  CREATED = "created",
  VERIFYING = "verifying",
  VERIFIED = "verified",
  FAILED = "failed",
  OUTDATED = "outdated",
}
